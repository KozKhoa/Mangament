export default interface FilterProps {
  label: string; // label is used to display
  code?: string; // code is what is used to call api
  isChecked: boolean; // isChecked is used to dertermine this field being checked or not
}
